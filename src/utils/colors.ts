import colors from '../assets/colors.json';

interface Icolors {
  "background_light": string,
  "background_dark": string,
  "background_dark_2": string,
  "text_light": string,
  "text_dark": string,
  "text_disabled": string,
  "primary": string
}

export default colors as Icolors;